/**
  This configuration file is for global variables, that don't differ between environments.
*/
module.exports = {
    sessionSecret: null //replace with a string with random characters. This is used to hash the cookie.
  , sessionCookieKey: 'blah.sid' //this is what the cookie is named
}

